[build]
builder = "NIXPACKS"

[phases.deploy]
startCommand = "cd /app/src && uvicorn api.main:app --host 0.0.0.0 --port 8000"

[phases.setup]
cmds = ["pip install -r infra/requirements.txt"]

[[deploy]]
strategy = "rolling"
instances = 1
envVars = [
    { key = "PORT", value = "8000" }
]
